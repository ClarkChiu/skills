# ig-reel 實作計畫

> 依 `docs/specs/2026-07-02-ig-reel-design.md`。批次指令下自主執行。取捨沿用前例：配方類 reference 給完整可跑指令；零腳本。

1. **`ig-reel/SKILL.md`**（英文＋中文觸發語）：frontmatter（name/description/allowed-tools: Read, Write, Edit, Bash, Glob, AskUserQuestion）；六階段工作流摘要；鐵則（素材是使用者的／fail loud ffprobe／安全區＋字級地板／最少重編碼）；外部工具矩陣（ffmpeg 必備、auto-editor/whisper 選用、絕不自動安裝）；邊界（vs social-card/slide-deck、不發文、不抓庫存）。驗收：frontmatter 三欄、grep 鐵則。
2. **`references/pipeline.md`**：六階段完整 ffmpeg 配方（盤點 ffprobe JSON、粗剪 stream copy、合成 concat、輸出參數、批次迴圈殼、ffprobe 驗證步含預期輸出）。
3. **`references/reframe.md`**：9:16 決策表＋兩配方（中心裁切 `crop=ih*9/16:ih`＋縮放；模糊墊底 split→scale+boxblur 背景→overlay 前景）＋Reels 安全區數字（上 250／下 420 @1080×1920，與 social-card platform-specs 同源）。
4. **`references/subtitles-audio.md`**：whisper→SRT→`subtitles=` 燒錄（force_style：字級 ≥48、MarginV 進安全區、描邊）；混音（amix／sidechaincompress）＋`loudnorm`。
5. **`evals/evals.json`** 五案（對應設計 §5）。
6. **attribution.md＋sources.lock**：原創建構；管線形狀經 `research/2026-06-21-video-editing-projects-for-ig.md` 參考 MoneyPrinterTurbo/ShortGPT（未直接稽核＝不釘 lock，attribution 記載＋日後真要參考程式再稽核）；lock 釘 huashu-design `0e7ec8aca005`（HTML 動畫→MP4 為日後擴充線）。
7. **端到端驗證**：ffmpeg 合成測試片（testsrc2 1280×720＋sine 音）→ 走模糊墊底配方 → ffprobe 驗 1080×1920/H.264/yuv420p/有音軌。
8. **登錄**：apm.yml、README 表＋外部 CLI 節（ffmpeg/auto-editor/whisper）、skill-map（工具＋邊界表）、symlink。
9. **提交**：`✨ feat(ig-reel): 庫存素材→IG 9:16 短片技能（ffmpeg 核心、六階段管線）`＋登錄與 docs 提交；更新記憶 `ig-video-skill-idea`（已動工→已建）。
